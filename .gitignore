lib-cov
*.seed
*.log
*.csv
!test/fixtures/*.csv
*.dat
*.out
*.pid
*.gz

pids
logs
results

npm-debug.log

*~
node_modules

.idea/


trial

test/coverage.html

coverage

dist
dist-test

package-lock.json
yarn.lock

# Third-party conformance corpus, fetched on demand by
# scripts/fetch-reworkcss-tests.sh (never committed).
test/reworkcss-css

# SimonSapin/css-parsing-tests, if cloned here for reference. It measures CSS
# Syntax Level 3 error recovery, which this plugin deliberately does not claim
# (see Conformance in AGENTS.md), so no suite reads it. Third-party: never
# committed.
test/css-parsing-tests

# Vale's downloaded style packages. The vocabulary under
# .vale/styles/config/ and the house rule under .vale/styles/Tabnas/
# are ours and ARE committed; Google/ arrives from the pinned URL
# in .vale.ini via `vale sync`.
/.vale/styles/Google/

# Written by the counts script while measuring, removed after.
.vale-counts.ini

# Rust build output and lockfile (a library crate; the lockfile is not the
# dependency contract — Cargo.toml is, and this crate declares no
# dependencies at all).
rs/target
rs/Cargo.lock

.DS_Store
