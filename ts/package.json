{
  "name": "@tabnas/css",
  "version": "0.5.5",
  "description": "This plugin allows the [Tabnas](https://github.com/tabnas/parser) JSON parser to parse CSS (Cascading Style Sheets) into a reworkcss-style abstract syntax tree.",
  "main": "dist/css.js",
  "type": "commonjs",
  "browser": "css.min.js",
  "types": "dist/css.d.ts",
  "homepage": "https://github.com/tabnas/css",
  "keywords": [
    "css",
    "stylesheet",
    "tabnas",
    "parser",
    "object",
    "json"
  ],
  "author": "Richard Rodger (http://richardrodger.com)",
  "repository": {
    "type": "git",
    "url": "git://github.com/tabnas/css.git",
    "directory": "ts"
  },
  "scripts": {
    "fetch-reworkcss-or-warn": "bash ../scripts/fetch-reworkcss-tests.sh || echo 'WARNING: reworkcss corpus fetch FAILED; the conformance suites will FAIL LOUDLY (they never skip)'",
    "pretest": "npm run build && npm run fetch-reworkcss-or-warn",
    "test": "node --enable-source-maps --test \"dist-test/*.test.js\"",
    "pretest-some": "npm run build && npm run fetch-reworkcss-or-warn",
    "test-some": "node --enable-source-maps --test-name-pattern=\"$npm_config_pattern\" --test \"dist-test/*.test.js\"",
    "pretest-watch": "npm run build",
    "test-watch": "node --test --watch dist-test/css.test.js",
    "embed": "node embed-grammar.js",
    "install-reworkcss-tests": "bash ../scripts/fetch-reworkcss-tests.sh",
    "watch": "tsc --build src test -w",
    "build": "node embed-grammar.js && tsc --build src test",
    "clean": "rm -rf node_modules dist dist-test yarn.lock package-lock.json",
    "reset": "npm run clean && npm i && npm run build && npm test",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: ts/v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag ts/v$REPO_VERSION && git push origin ts/v$REPO_VERSION;",
    "repo-publish": "npm run clean && npm i && npm run repo-publish-quick",
    "repo-publish-quick": "npm run build && npm run test && npm run repo-tag",
    "repo-stage": "npm run clean && npm i && npm run repo-stage-quick",
    "repo-stage-quick": "npm run build && npm run test && npm stage publish",
    "repo-stage-list": "npm stage list",
    "repo-stage-approve": "npm stage approve"
  },
  "license": "MIT",
  "files": [
    "src",
    "dist",
    "LICENSE"
  ],
  "peerDependencies": {
    "@tabnas/jsonic": ">=0",
    "@tabnas/parser": ">=0"
  },
  "devDependencies": {
    "@tabnas/debug": "*",
    "@tabnas/jsonic": "*",
    "@tabnas/parser": "*",
    "@tabnas/railroad": "*",
    "@tabnas/support": "*",
    "@types/node": "25.6.0",
    "typescript": "6.0.3"
  },
  "engines": {
    "node": ">=24"
  }
}
